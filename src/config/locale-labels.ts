import type { SupportedLocale } from "@/config/app-config";

/** Native display names for the language selector dropdown. */
export const localeLabels: Record<SupportedLocale, string> = {
  en: "English",
  ru: "Русский",
  de: "Deutsch",
};
